{
  "name": "redstars-helper",
  "version": "0.1.0",
  "description": "Local helper agent for Redstars demo — wraps helper.py + tray + auto-update",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
