{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the helper agent",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "opener:default",
    "opener:allow-open-url",
    "updater:default",
    "process:default"
  ]
}
